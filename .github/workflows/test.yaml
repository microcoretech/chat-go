name: Test

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  verify:
    name: Verify
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Verify
        run: make verify